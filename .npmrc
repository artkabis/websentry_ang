# Les workspaces se résolvent via le protocole workspace: uniquement.
link-workspace-packages=true
prefer-workspace-packages=true
# Empêche une install silencieuse si le lockfile diverge (miroir du --frozen-lockfile CI).
strict-peer-dependencies=false
